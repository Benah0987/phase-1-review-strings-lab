// Write your code in this file!
// index.js

//  current user as a string
const currentUser = 'John';

// welcome message using template literals
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Define the excited welcome message using template literals and the toUpperCase() method
const excitedWelcomeMessage = `WELCOME TO FLATBOOK, ${currentUser.toUpperCase()}!`;

// Define the short greeting using template literals and the substring method, and an exclamation mark
const shortGreeting = `Welcome, ${currentUser[0]}!`;


